services:
  postgres:
    image: postgres:16-alpine
    container_name: escuela-cocina-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: cocina
      POSTGRES_PASSWORD: cocina
      POSTGRES_DB: escuela_cocina
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
